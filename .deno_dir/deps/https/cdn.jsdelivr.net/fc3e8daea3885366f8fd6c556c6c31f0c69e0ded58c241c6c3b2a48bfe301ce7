import { path } from "./deps.js";

export const defaults = () => {
    return {
        sourcePath: path.join(Deno.cwd(), 'src'),
        componentsPath: path.join(Deno.cwd(), 'src', 'components'),
        targetPath: path.join(Deno.cwd(), 'docs'),
        forceRebuild: false
    };
};
